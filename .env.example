# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/linkedin_clone

# JWT
JWT_SECRET=your_jwt_secret_here

# Client URL (used when sending emails and for CORS in development)
CLIENT_URL=http://localhost:5173

# Mailtrap (email testing)
MAILTRAP_TOKEN=your_mailtrap_token_here
EMAIL_FROM=mailtrap@example.com
EMAIL_FROM_NAME=YourName
VITE_API_BASE_URL=http://localhost:5000/api/v1
# File uploads
# (Files are stored in backend/uploads by default)
